spring:
  application:
    name: "backend"

  datasource:
    url: "jdbc:mysql://localhost:3306/plan-a-meal"
    username: "root"
    password: "root"
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true